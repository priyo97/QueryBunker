<%= form_for(@problem_statement) do |f| %>
  <% if @problem_statement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@problem_statement.errors.count, "error") %> prohibited this problem_statement from being saved:</h2>

      <ul>
      <% @problem_statement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_area :question %>
  </div>
  <div class="field">
    <%= f.label :answer %><br>
    <%= f.text_area :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
